# Prettier should not format md files, as we use `markdownlint` for that purpose
*.md
dist/
pnpm-lock.yaml

# Codegen snapshot — emitted by hey-api/openapi-ts; reformatting it would
# distort the test signal (the whole point is byte-stability against the
# plugin's actual emit).
**/test/__generated__/
